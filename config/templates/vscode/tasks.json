{
    // Those are default snippets provided by the framework, you can modify them to meet your specific needs.
    
    "version": "2.0.0",
    "tasks": [
      {
        "label": "compile",
        "type": "shell",
        "command": "node",
        "args": ["bin/cli", "compile", "\"${relativeFile}\"","\"${file}\""],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "presentation": {
          "reveal": "always",
          "panel": "new"
        },
        "problemMatcher": []
      }
    ],
    "inputs": [
    ]
  }